{
    "image_api_base": {
        "description": "中转站地址（如 https://www.msuicode.com，不要带 /v1 路径）",
        "type": "string",
        "default": "https://www.msuicode.com"
    },
    "image_api_key": {
        "description": "中转站 API Key",
        "type": "string",
        "default": ""
    },
    "model": {
        "description": "画图模型名称",
        "type": "string",
        "default": "gpt-image-2"
    },
    "timeout": {
        "description": "请求超时时间（秒）",
        "type": "int",
        "default": 600
    }
}
