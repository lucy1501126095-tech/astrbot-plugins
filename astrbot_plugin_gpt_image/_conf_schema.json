{
  "api_mode": {
    "description": "画图模式：images = 文生图（调用 /v1/images/generations），edits = 图生图（调用 /v1/images/edits，可以基于已有图片修改）",
    "type": "string",
    "default": "images"
  },
  "model": {
    "description": "画图模型名称",
    "type": "string",
    "default": "gpt-image-2"
  },
  "image_api_base": {
    "description": "中转站地址（如 https://xxx.com，不要带 /v1/images/... 后缀）",
    "type": "string",
    "default": ""
  },
  "image_api_key": {
    "description": "中转站 API Key",
    "type": "string",
    "default": ""
  },
  "timeout": {
    "description": "画图请求超时时间（秒）",
    "type": "int",
    "default": 120
  }
}
